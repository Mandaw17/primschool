{% extends "base.html.twig" %}

{% block body %}
       <center><h1>Demander RV</h1></center>
         {% if app.user %}
            {% if app.user.role is same as('parent')%}
                <div class="container">

                        <div class="form-group">
                            {{form_start(form)}}
                            {{form_widget(form)}}
                            {{form_end(form)}}
                        </div>
                </div>
            {% else %}
                <a href="/" class="btn btn-outline-primary">Retour</a>
            {% endif %}
        {% else %}
            <a href="/" class="btn btn-outline-primary">Retour</a>
        {% endif %}
{% endblock %}